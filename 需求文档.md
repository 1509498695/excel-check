# 📄 企业级配置表自动化检查与动态规则校验工作台 - 系统详细设计说明书 (SDD)

## 0. 文档说明

- 文档名称：企业级配置表自动化检查与动态规则校验工作台 - 系统详细设计说明书
- 当前版本：V4.6
- 修订日期：2026-04-11 13:06
- 修订依据：
  - `README.md`
  - `PROJECT_RECORD.md`
  - `CHANGELOG.md`
  - `frontend/README.md`
  - 当前前后端代码实现
- 本版目标：将 SDD 更新为与当前仓库真实实现一致的“现状版详细设计”，替换旧文档中已经过期的技术栈、模块边界、接口说明和页面结构。

## 0.1 修订记录

| 版本 | 日期 | 摘要 | 修订人 |
|:---|:---|:---|:---|
| V1.0 | 202X-XX-XX | 初版架构设计 | 架构组 |
| V2.0 | 202X-XX-XX | 补充状态流转、规则引擎和 API 协议 | 架构组 |
| V3.0 | 202X-XX-XX | 深化编码级设计说明 | 架构组 |
| V4.0 | 2026-04-08 | 按当前代码重写：统一为 `FastAPI + Vue 3 + Pinia + Element Plus + Vue Router`，补充固定规则模块、规则组、规则级文件绑定、配置迁移和当前联调基线 | Codex |
| V4.1 | 2026-04-09 | 补充固定规则模块的 SVN CLI 自动探测、环境变量覆盖和当前 Windows 真机联调结果 | Codex |
| V4.2 | 2026-04-10 | 固定规则弹窗中的 `比较符` 更名为 `规则选择`，补充 `not_null / unique` 两类固定规则、`version = 3` 配置协议与旧版比较型配置自动迁移 | Codex |
| V4.3 | 2026-04-10 | 固定规则弹窗新增默认规则名自动生成与手动改名保护；`unique` 的用户可见文案统一收口为 `唯一校验` | Codex |
| V4.4 | 2026-04-10 | 固定规则页新增独立的数据源接入管理与变量池构建，配置升级为 `version = 4`，规则绑定改为 `target_variable_tag`，实现与主工作台完全隔离的持久化闭环 | Codex |
| V4.5 | 2026-04-11 | 修复变量池复用面板中的 `????` 文案乱码，恢复主工作台与固定规则页的中文按钮、表头、弹窗和详情区显示 | Codex |
| V4.6 | 2026-04-11 | 固定规则配置读取新增失效路径容错与 `config_issues` 告警回传；固定规则页在失效路径场景下可继续打开并引导到数据源接入管理修复，保存与执行继续保持严格校验 | Codex |

## 0.2 本版修订摘要

- 后端实际框架为 `FastAPI`，不再是旧版文档中的 `Flask`
- 前端状态管理实际为 `Pinia`，不再是 `Vuex`
- 前端组件库实际为 `Element Plus`，不再混写 `Ant Design`
- 前端已经不是单一页面，当前有两个路由：
  - `/`：四步工作台
  - `/fixed-rules`：固定规则检查页
- 步骤 3 当前已收口为纯静态规则工作区，动态规则配置 UI 已隐藏
- 新增固定规则模块：
  - 接口前缀：`/api/v1/fixed-rules/*`
  - 单独路由：`/fixed-rules`
  - 配置持久化：`backend/.runtime/fixed-rules/default.json`
  - 当前配置结构版本：`version = 4`
  - 固定规则页拥有自己的 `sources / variables / groups / rules` 持久化配置
  - 规则绑定改为 `target_variable_tag`，当前固定规则执行只消费单变量
  - 固定规则页当前支持 `fixed_value_compare / not_null / unique`
  - 新增规则时，`规则名称` 会按 `sheet-目标列-规则选择名称(+值)` 自动生成，并在用户未手改时随表单同步
  - 固定规则页顶部复用了工作台的数据源接入管理与变量池构建 UI，但底层状态与主工作台完全隔离
- 当前固定规则模块的 `svn` 能力已支持显式配置、`PATH` 与 Windows TortoiseSVN 自动探测，并已在有效工作副本上完成真实更新
- 当前 `feishu` 读取仍为占位实现
- 变量池复用面板中的中文文案已恢复，主工作台与固定规则页不再显示 `????` 占位文本
- 固定规则配置读取现在会把“本地路径失效 / Sheet 不存在 / 列不存在”作为非阻断问题返回到 `meta.config_issues`，固定规则页可继续加载并展示中文 warning；保存和执行仍保持明确中文 `400`

---

## 1. 系统现状总览

## 1.1 产品定位

本系统是一个本地运行的配置表校验工作台，面向策划表、配置表、掉落表、商店表等 Excel/CSV 数据源的规则校验场景。当前实现聚焦两个使用入口：

1. 四步工作台：适合临时编排数据源、变量和规则后立即执行
2. 固定规则检查页：适合长期维护固定规则页自己的数据源、变量池、规则组和固定规则后重复执行

## 1.2 当前真实技术栈

| 分层 | 当前实现 | 说明 |
|:---|:---|:---|
| 前端框架 | Vue 3 + TypeScript | 单页应用 |
| 前端路由 | Vue Router | 当前 2 个主路由 |
| 前端状态 | Pinia | `workbench` 与 `fixed-rules` 两套 Store |
| 前端组件库 | Element Plus | 表单、弹窗、表格、分页、消息提示 |
| 前端构建 | Vite | 本地开发代理到 `127.0.0.1:8000` |
| 后端框架 | FastAPI | REST API、Pydantic 校验 |
| 数据处理 | Pandas | Excel/CSV 读取与规则计算 |
| Excel 依赖 | `openpyxl` / `xlrd` | `.xlsx` 用 `openpyxl`，`.xls` 用 `xlrd` |
| 本地文件选择 | `tkinter` | 由后端弹出系统文件选择框 |
| SVN 调用 | `subprocess` + CLI | 当前依赖本机 `svn.exe` 或 `PATH` |

## 1.3 当前能力边界

### 已落地

- 本地 Excel / CSV 数据源接入
- Excel 元数据读取
- 变量池构建
- 组合变量 JSON 预览与持有
- 静态规则执行：
  - `not_null`
  - `unique`
  - `cross_table_mapping`
- 固定规则模块执行：
  - `fixed_value_compare`
  - `not_null`
  - `unique`
  - 其中比较型规则继续使用操作符 `eq / ne / gt / lt`
- 固定规则页自己的数据源接入管理与变量池构建
- 固定规则页 `version = 4` 持久化配置与旧版 `version = 3` 自动迁移
- 统一结果看板
- 固定规则配置持久化与自动回填
- 固定规则规则组、规则 CRUD、按目标变量选规
- 固定规则模块的工作副本 `SVN 更新`

### 已保留接口但未完全打通

- `regex` 规则类型已注册，但当前仍返回空结果，占位未闭环
- `svn` 作为主工作台独立 `source` 类型的完整闭环仍未开放；当前已打通的是固定规则模块的工作副本更新
- `feishu` 读取入口已保留，但加载器仍抛出 `NotImplementedError`

### 当前明确未开放

- 步骤 3 的动态规则配置 UI
- 固定规则多配置集切换
- 固定规则结果导出
- CSV 的变量池元数据下拉提取

---

## 2. 总体架构设计

## 2.1 逻辑架构

| 层级 | 模块 | 职责 |
|:---|:---|:---|
| 展示层 | `MainBoard.vue`、`FixedRulesBoard.vue` | 组织页面布局、触发用户操作 |
| 状态层 | `frontend/src/store/*.ts` | 管理数据源、变量、规则、结果、分页和缓存 |
| API 层 | `frontend/src/api/*.ts` | 统一封装前后端 HTTP 通信 |
| 接口层 | `backend/app/api/*.py` | 暴露 REST 接口、参数校验、异常转 HTTP |
| 服务层 | `backend/app/fixed_rules/service.py` | 固定规则配置持久化、迁移、编排执行 |
| 适配层 | `backend/app/loaders/*.py` | 本地 Excel/CSV 读取、元数据提取、SVN 调用、飞书占位 |
| 规则层 | `backend/app/rules/*.py` | 注册规则、执行规则、返回统一异常结果 |
| 工具层 | `backend/app/utils/*.py` | 响应格式化、基础工具 |

## 2.2 当前模块划分

### 主工作台 `/`

1. 步骤 1：数据源接入管理
2. 步骤 2：变量池构建
3. 步骤 3：静态规则编排
4. 步骤 4：结果看板

### 固定规则页 `/fixed-rules`

1. 顶部操作条
2. 数据源接入管理
3. 变量池构建
4. 规则组导航带
5. 当前组规则列表
6. 结果与 SVN 反馈区

## 2.3 当前目录结构

### 前端

```text
frontend
├── src
│   ├── api
│   │   ├── fixedRules.ts
│   │   └── workbench.ts
│   ├── components
│   │   ├── fixed-rules
│   │   │   └── FixedRulesResultPanel.vue
│   │   └── workbench
│   │       ├── DataSourcePanel.vue
│   │       ├── ResultBoardPanel.vue
│   │       ├── RuleComposerPanel.vue
│   │       ├── SectionBlock.vue
│   │       └── VariablePoolPanel.vue
│   ├── router
│   │   └── index.ts
│   ├── store
│   │   ├── fixedRules.ts
│   │   └── workbench.ts
│   ├── types
│   │   ├── fixedRules.ts
│   │   └── workbench.ts
│   ├── utils
│   │   ├── taskTree.ts
│   │   └── workbenchMeta.ts
│   ├── views
│   │   ├── FixedRulesBoard.vue
│   │   └── MainBoard.vue
│   ├── App.vue
│   ├── main.ts
│   ├── style.css
│   └── fixed-rules.css
├── package.json
└── vite.config.ts
```

### 后端

```text
backend
├── app
│   ├── api
│   │   ├── execute_api.py
│   │   ├── fixed_rules_api.py
│   │   ├── fixed_rules_schemas.py
│   │   ├── router.py
│   │   ├── schemas.py
│   │   └── source_api.py
│   ├── fixed_rules
│   │   ├── schemas.py
│   │   └── service.py
│   ├── loaders
│   │   ├── feishu_reader.py
│   │   ├── local_reader.py
│   │   └── svn_manager.py
│   ├── rules
│   │   ├── engine_core.py
│   │   ├── rule_basics.py
│   │   ├── rule_cross.py
│   │   └── rule_fixed.py
│   ├── utils
│   │   ├── data_cleaner.py
│   │   └── formatter.py
│   └── __init__.py
├── config.py
├── requirements.txt
└── run.py
```

---

## 3. 前端详细设计

## 3.1 应用外壳

`App.vue` 当前提供统一头部导航，包含：

- 品牌区：`Excel Check`
- 导航项：
  - 工作台
  - 固定规则检查

路由定义如下：

```ts
routes: [
  { path: '/', component: MainBoard },
  { path: '/fixed-rules', component: FixedRulesBoard },
]
```

## 3.2 主工作台页面设计

`MainBoard.vue` 采用四段式工作流设计。

### 步骤 1：数据源接入

由 `DataSourcePanel.vue` 负责。

当前职责：

- 新增、编辑、删除数据源
- 调用本地文件选择接口
- 保存本地 Excel / CSV 路径
- 清理相关元数据缓存

### 步骤 2：变量池构建

由 `VariablePoolPanel.vue` 负责。

当前支持两类变量：

- 单变量 `variable_kind = single`
- 组合变量 `variable_kind = composite`

单变量字段：

- `source_id`
- `sheet`
- `column`
- `tag`
- `expected_type`

组合变量字段：

- `source_id`
- `sheet`
- `columns`
- `key_column`
- `tag`
- `expected_type = json`

说明：

- 组合变量当前只支持同一数据源、同一 Sheet、多列组合
- 组合变量会进入 `TaskTree.variables`
- 但当前静态规则不消费组合变量

### 步骤 3：规则编排

由 `RuleComposerPanel.vue` 负责。

当前现状：

- 页面只展示静态规则模板
- 动态规则配置 UI 已隐藏
- 执行请求只会提交静态规则
- `mode === 'dynamic'` 的数据结构兼容仍保留在前端 Store 中，但不进入最终执行

当前可见静态规则模板：

- `not_null`
- `unique`
- `cross_table_mapping`

### 步骤 4：结果看板

由 `ResultBoardPanel.vue` 负责。

当前展示：

- 扫描总行数
- 执行耗时
- failed sources
- 异常结果表

## 3.3 固定规则页面设计

`FixedRulesBoard.vue` 当前已调整为“上下结构”。

### 顶部操作条

固定展示以下按钮：

- 保存配置
- SVN 更新
- 执行全部规则

并展示整体统计：

- 规则组数量
- 规则总数
- 固定路径数
- 异常结果数

### 规则组导航带

上方横向展示规则组胶囊导航，包含：

- 搜索框
- 新建规则组
- 每组规则数量 badge
- 失效组提示
- 当前激活态

规则组规则：

- 默认组：`未分组`
- 默认组不可删除、不可改名
- 删除自定义组时，组内规则自动迁移到 `未分组`

### 当前组规则区

下方展示当前选中组的规则列表。

每条规则直接展示：

- 规则名称
- 绑定文件摘要
- `sheet / column`
- 规则选择摘要
- 条件值（仅比较类规则）

支持：

- 新增规则
- 编辑规则
- 删除规则
- 分页展示

分页参数：

- 固定每页 `20` 条

### 规则编辑弹窗

每次新增或编辑规则时，必须单独配置：

- 规则组
- 规则名称
- 目标变量
- 规则选择
- 比较值（仅比较类规则显示）

当前不再手工维护规则级文件路径，而是直接从固定规则页变量池选择目标变量。
选中目标变量后，页面会展示只读摘要：

- 来源数据源
- Sheet
- 列名
- 路径

规则名称交互规则：

- 新增规则时，默认按 `sheet-目标列-规则选择名称(+值)` 自动生成
- 比较类规则带 `+值`，非比较类不带
- 只要用户还未手动修改规则名称，`目标变量 / 规则选择 / 比较值` 变化后会自动刷新默认名
- 一旦用户手动改名或主动清空规则名称，后续字段变化不再自动覆盖
- 规则名称仍为必填，空规则名不能保存

当前规则选择项固定为：

- `等于 (=)`
- `不等于 (!=)`
- `大于 (>)`
- `小于 (<)`
- `非空校验`
- `唯一校验`

其中：

- 比较类规则保存为 `rule_type = fixed_value_compare`
- `非空校验` 保存为 `rule_type = not_null`
- `唯一校验` 保存为 `rule_type = unique`
- `not_null / unique` 不展示也不保存 `expected_value`
- 当前固定规则执行只允许绑定单变量；组合变量继续保留在固定规则页变量池中做持久化、编辑和预览

## 3.4 前端状态设计

### `workbench` Store

主要状态：

- `sources`
- `variables`
- `rules`
- `capabilities`
- `abnormalResults`
- `executionMeta`
- `sourceMetadataMap`
- `variablePreviewMap`

主要职责：

- 构建四步工作台的 `TaskTree`
- 缓存 Excel 元数据和列预览
- 维护规则引用替换
- 执行主工作台规则

### `fixed-rules` Store

主要状态：

- `config`
- `capabilities`
- `preferredSourceId`
- `activeTag`
- `sourceMetadataMap`
- `variablePreviewMap`
- `selectedGroupId`
- `groupKeyword`
- `currentPage`
- `executionMeta`
- `abnormalResults`
- `svnUpdateResults`

主要职责：

- 读取 / 保存固定规则配置
- 管理固定规则页自己的数据源、变量、规则组与规则 CRUD
- 缓存固定规则页数据源的元数据与变量预览
- 删除数据源时级联删除变量和绑定规则
- 删除变量时自动清理绑定规则，变量改名时同步替换规则引用
- 执行全部固定规则
- 聚合显示 SVN 更新结果

---

## 4. 后端详细设计

## 4.1 启动与应用装配

`backend/run.py` 为后端启动入口。

当前行为：

1. 创建 `FastAPI` 实例
2. 注册 `api_router`
3. 暴露 `/health`
4. 用 `uvicorn.run` 按配置启动

`backend/config.py` 当前核心配置：

| 字段 | 当前值 / 作用 |
|:---|:---|
| `app_name` | `excel-check-backend` |
| `host` | `127.0.0.1` |
| `port` | `8000` |
| `api_v1_prefix` | `/api/v1` |
| `default_thread_pool_size` | `4` |
| `fixed_rules_config_path` | `backend/.runtime/fixed-rules/default.json` |
| `svn_executable` | 默认读取环境变量 `SVN_EXECUTABLE`，未设置时回退为 `svn` |
| `supported_source_types` | `local_excel / local_csv / feishu / svn` |

## 4.2 API 路由装配

`backend/app/api/router.py` 当前聚合三个子路由：

- `source_router`
- `execute_router`
- `fixed_rules_router`

## 4.3 数据读取层设计

### `local_reader.py`

职责：

- 读取本地 Excel / CSV
- 提取 Excel 元数据
- 预览单列数据
- 预览组合变量 JSON
- 按变量切片装载 DataFrame

关键实现：

- `.xlsx` 使用 `openpyxl`
- `.xls` 使用 `xlrd`
- 通过 `usecols` 只读取目标列，控制内存
- 单变量载入时统一追加 `_row_index = index + 2`
- 组合变量会构造成 `json_value` 单列表结构

### `svn_manager.py`

职责：

- 探测 `svn` 可执行文件
- 调用 `svn update`
- 对“不是工作副本”“CLI 缺失”等情况给出明确错误

当前特点：

- 依赖 `settings.svn_executable`
- 优先支持显式配置路径，其次尝试 `PATH`
- 在 Windows 下会自动探测 TortoiseSVN 注册表与常见安装路径
- 当前机器实测命中：`C:\Program Files\TortoiseSVN\bin\svn.exe`
- 固定规则页的 SVN 更新最终也走这里

### `feishu_reader.py`

当前状态：

- 占位实现
- 当前直接抛出 `NotImplementedError`

## 4.4 执行引擎设计

`execute_api.py` 是主工作台的统一执行入口。

主流程：

1. 校验 `TaskTree`
2. 按 `source_id` 聚合变量
3. 用线程池并发加载数据源
4. 对失败的数据源做降级记录
5. 过滤依赖失败源的规则
6. 调用规则注册表执行规则
7. 统一组装返回结构

### 并发加载策略

当前使用：

- `ThreadPoolExecutor(max_workers=settings.default_thread_pool_size)`

适用场景：

- 多数据源并发读取
- SVN 同步 + 本地文件读取

### 规则执行入口

`engine_core.py` 当前使用注册表模式：

```python
RULE_REGISTRY: dict[str, RuleHandler] = {}
```

执行时按 `task_tree.rules` 顺序遍历：

1. 查找 `rule_type`
2. 获取 handler
3. 传入 `RuleExecutionContext`
4. 汇总 `abnormal_results`

## 4.5 固定规则服务层设计

`fixed_rules/service.py` 是固定规则模块的核心服务层。

主要职责：

- 构建默认配置
- 读取持久化配置
- 兼容旧版配置迁移
- 校验与标准化配置
- 生成临时 `TaskTree`
- 触发执行
- 聚合固定路径的 SVN 更新

### 当前配置版本

- 当前版本：`version = 4`

### 当前持久化路径

- `backend/.runtime/fixed-rules/default.json`

### 旧版迁移策略

- 若发现旧版顶层字段：
  - `file_path`
  - `sheet`
  - `columns`
  - `svn_enabled`
  则先迁移为规则级结构，再继续规范化为当前 `version = 4` 配置。
- 若检测到 `version = 3` 或规则仍使用 `binding.file_path / binding.sheet / binding.column`
  - 服务层会自动去重生成 `sources`
  - 自动生成或复用对应单变量 `variables`
  - 将规则改写为 `target_variable_tag`
  - 最终统一保存为 `version = 4`

### 固定规则执行编排

固定规则不会复刻第二套执行引擎，而是：

1. 从已保存配置读取规则
2. 根据规则引用的 `target_variable_tag` 聚合出临时 `sources`
3. 根据规则引用的 `target_variable_tag` 聚合出临时 `variables`
4. 按组顺序和规则顺序生成 `ValidationRule`
5. 复用现有 `execute_engine`

这样可以确保：

- 主工作台和固定规则页使用相同结果结构
- 规则执行逻辑只维护一套

---

## 5. 核心数据模型

## 5.1 主工作台协议

### DataSource

```json
{
  "id": "src_demo",
  "type": "local_excel",
  "path": "D:/path/demo.xlsx",
  "pathOrUrl": "D:/path/demo.xlsx",
  "url": null,
  "token": null
}
```

字段说明：

| 字段 | 说明 |
|:---|:---|
| `id` | 数据源唯一标识 |
| `type` | `local_excel / local_csv / feishu / svn` |
| `path` | 本地文件或 SVN 文件路径 |
| `pathOrUrl` | 兼容字段 |
| `url` | 飞书地址 |
| `token` | 飞书 token |

### VariableTag

```json
{
  "tag": "[src_items-items-INT_ID]",
  "source_id": "src_items",
  "sheet": "items",
  "variable_kind": "single",
  "column": "INT_ID",
  "columns": null,
  "key_column": null,
  "expected_type": "str"
}
```

### ValidationRule

```json
{
  "rule_id": "rule-1",
  "rule_type": "cross_table_mapping",
  "params": {
    "dict_tag": "[items-id]",
    "target_tag": "[drops-ref]"
  }
}
```

### TaskTree

```json
{
  "sources": [],
  "variables": [],
  "rules": []
}
```

## 5.2 固定规则模块协议

当前固定规则协议已升级为固定规则页自己的持久化配置。

```json
{
  "version": 4,
  "configured": true,
  "sources": [
    {
      "id": "fixed-src-demo",
      "type": "local_excel",
      "path": "D:/project/excel-check/.codex/runtime/fixed_rules_flow.xlsx"
    }
  ],
  "variables": [
    {
      "tag": "[fixed-src-demo-items-INT_ID]",
      "source_id": "fixed-src-demo",
      "sheet": "items",
      "variable_kind": "single",
      "column": "INT_ID",
      "expected_type": "str"
    },
    {
      "tag": "[fixed-src-demo-items-INT_ID-mapping]",
      "source_id": "fixed-src-demo",
      "sheet": "items",
      "variable_kind": "composite",
      "columns": [
        "STR_ItemID",
        "DESC"
      ],
      "key_column": "INT_ID",
      "expected_type": "json"
    }
  ],
  "groups": [
    {
      "group_id": "ungrouped",
      "group_name": "未分组",
      "builtin": true
    },
    {
      "group_id": "basic-checks",
      "group_name": "基础校验",
      "builtin": false
    }
  ],
  "rules": [
    {
      "rule_id": "rule-int-id-gt-0",
      "group_id": "basic-checks",
      "rule_name": "INT_ID 必须大于 0",
      "target_variable_tag": "[fixed-src-demo-items-INT_ID]",
      "rule_type": "fixed_value_compare",
      "operator": "gt",
      "expected_value": "0"
    },
    {
      "rule_id": "rule-int-id-not-null",
      "group_id": "basic-checks",
      "rule_name": "INT_ID 不能为空",
      "target_variable_tag": "[fixed-src-demo-items-INT_ID]",
      "rule_type": "not_null"
    }
  ]
}
```

说明：

- `sources / variables / groups / rules` 统一由 `/api/v1/fixed-rules/config` 持久化
- `target_variable_tag` 用于把固定规则绑定到固定规则页自己的单变量
- 组合变量可以保存在 `variables` 中，但当前固定规则执行不会直接消费组合变量
- `fixed_value_compare` 需要 `operator + expected_value`
- `not_null / unique` 不需要 `operator` 与 `expected_value`

### 固定规则排序规则

执行时按以下顺序展开：

1. 先按 `groups` 数组顺序
2. 再按该组下规则在 `rules` 数组中的出现顺序

### 固定规则内部变量引用

后端执行时会直接复用已保存的 `VariableTag.tag`，不再额外生成新的内部文件绑定标签。

---

## 6. 接口设计

## 6.1 系统接口

### `GET /health`

用途：

- 健康检查
- 联调连通性确认

返回示例：

```json
{
  "code": 200,
  "msg": "ok",
  "data": {
    "status": "healthy",
    "service": "excel-check-backend"
  }
}
```

## 6.2 数据源接口

### `GET /api/v1/sources/capabilities`

返回后端声明支持的数据源类型。

注意：

- 当前 `implemented` 字段仍为 `false`
- 这是能力声明接口，不代表所有 source type 都已闭环

### `POST /api/v1/sources/local-pick`

用途：

- 由后端弹出本地文件选择框

请求示例：

```json
{
  "source_type": "local_excel"
}
```

返回示例：

```json
{
  "code": 200,
  "msg": "ok",
  "data": {
    "selected_path": "D:/project/excel-check/backend/tests/data/minimal_rules.xlsx",
    "source_type": "local_excel"
  }
}
```

### `POST /api/v1/sources/metadata`

用途：

- 读取 Excel 的 Sheet 和列结构

当前边界：

- 仅支持 `local_excel`

### `POST /api/v1/sources/column-preview`

用途：

- 读取单变量列预览

当前返回字段：

- `preview_rows`
- `total_rows`
- `loaded_rows`
- `loaded_all_rows`
- `preview_limit`
- `source_path`

### `POST /api/v1/sources/composite-preview`

用途：

- 读取组合变量 JSON 预览

当前边界：

- 仅支持 Excel
- 同数据源同 Sheet

## 6.3 主执行接口

### `POST /api/v1/engine/execute`

用途：

- 执行主工作台规则

响应结构固定为：

```json
{
  "code": 200,
  "msg": "Execution Completed",
  "meta": {
    "execution_time_ms": 1105,
    "total_rows_scanned": 15000,
    "failed_sources": []
  },
  "data": {
    "abnormal_results": []
  }
}
```

## 6.4 固定规则接口

### `GET /api/v1/fixed-rules/config`

用途：

- 读取当前固定规则配置

特性：

- 配置不存在时返回默认空结构
- 读取到旧版配置时会先迁移再返回

### `PUT /api/v1/fixed-rules/config`

用途：

- 保存整份固定规则配置

保存前校验：

- 规则组合法
- 规则 ID 不重复
- 规则名称非空
- 绑定文件合法
- 绑定 Sheet 存在
- 绑定列存在
- 比较类规则必须提供 `operator`
- 比较类规则必须提供 `expected_value`
- `gt / lt` 的 `expected_value` 必须可转数值
- `not_null / unique` 不要求提供 `expected_value`

### `POST /api/v1/fixed-rules/svn-update`

用途：

- 聚合所有已配置规则的绑定路径
- 取父目录去重后执行统一 SVN 更新

返回示例结构：

```json
{
  "code": 200,
  "msg": "ok",
  "data": {
    "total_paths": 2,
    "updated_paths": 1,
    "results": [
      {
        "working_copy": "D:/xxx",
        "status": "success",
        "output": "Updated to revision ...",
        "used_executable": "C:/Program Files/TortoiseSVN/bin/svn.exe"
      }
    ]
  }
}
```

### `POST /api/v1/fixed-rules/execute`

用途：

- 执行当前全部固定规则

返回结构：

- 与主执行接口完全一致

---

## 7. 规则详细设计

## 7.1 `not_null`

作用：

- 检查单变量列中的空值

输入：

- `params.target_tags: string[]`

输出：

- `level = error`

## 7.2 `unique`

作用：

- 检查单变量列中的重复值

输入：

- `params.target_tags: string[]`

输出：

- `level = warning`

## 7.3 `cross_table_mapping`

作用：

- 检查目标列中的值是否都出现在基础字典列中

输入：

- `params.dict_tag`
- `params.target_tag`

输出：

- `level = error`

## 7.4 `fixed_value_compare`

作用：

- 固定规则模块的单列常量比较规则

输入：

- `params.target_tag`
- `params.operator`
- `params.expected_value`
- `params.rule_name`

操作符语义：

| 操作符 | 语义 |
|:---|:---|
| `eq` | 去首尾空白后精确等于 |
| `ne` | 去首尾空白后精确不等于 |
| `gt` | 数值大于 |
| `lt` | 数值小于 |

补充说明：

- `eq / ne` 用文本比较
- `gt / lt` 用数值比较
- 若数值比较遇到“非空但不可转数值”的单元格，会返回异常而不是跳过
- 固定规则页当前也可直接编排 `not_null` 与 `unique`，这两类规则复用基础规则实现，并分别保持 `error / warning` 语义

## 7.5 `regex`

当前状态：

- 已注册
- 暂未实现真实计算
- 当前返回空列表

---

## 8. 关键执行时序

## 8.1 主工作台时序

1. 用户在步骤 1 保存数据源
2. 用户在步骤 2 构建单变量或组合变量
3. 用户在步骤 3 选择静态规则模板并补齐参数
4. 前端调用 `buildTaskTreePayload`
5. 前端请求 `POST /api/v1/engine/execute`
6. 后端按 `source_id` 聚合变量
7. 后端线程池并发加载数据源
8. 后端过滤依赖失败源的规则
9. 规则引擎按顺序执行规则
10. 后端组装统一结果响应
11. 前端渲染步骤 4 结果看板

## 8.2 固定规则页时序

1. 页面打开调用 `GET /api/v1/fixed-rules/config`
2. Store 自动回填固定规则页自己的数据源、变量池、规则组和规则
3. 用户先维护固定规则页数据源与变量，再在弹窗中为单条规则选择目标变量、规则类型和比较值
4. 前端调用 `PUT /api/v1/fixed-rules/config` 保存配置
5. 用户点击“执行全部规则”
6. 前端先保存配置，再调用 `POST /api/v1/fixed-rules/execute`
7. 后端根据 `target_variable_tag` 聚合临时 `TaskTree`
8. 后端复用主执行引擎
9. 返回统一结果响应
10. 前端在固定规则页结果区渲染摘要和异常列表

## 8.3 固定规则配置迁移时序

1. 后端读取 `default.json`
2. 若检测到旧版顶层 `file_path / sheet / columns / svn_enabled`
3. 自动映射为过渡规则级结构
4. 再继续迁移为 `sources / variables / rules[*].target_variable_tag`
5. 最终按 `version = 4` 执行统一校验与标准化
5. 返回新版配置给前端

---

## 9. 结果模型与行号规则

所有执行入口统一返回：

```json
{
  "code": 200,
  "msg": "Execution Completed",
  "meta": {
    "execution_time_ms": 0,
    "total_rows_scanned": 0,
    "failed_sources": []
  },
  "data": {
    "abnormal_results": []
  }
}
```

### `abnormal_results` 结构

| 字段 | 说明 |
|:---|:---|
| `level` | `error / warning / ...` |
| `rule_name` | 规则名，固定规则优先使用用户配置的规则名称 |
| `location` | 定位信息 |
| `row_index` | Excel 实际行号口径 |
| `raw_value` | 原始单元格值 |
| `message` | 详细说明 |

### 行号口径

当前统一按：

```text
Pandas 索引 + 2
```

原因：

- Pandas 从 0 开始
- Excel 第 1 行通常为表头

---

## 10. 非功能设计与异常处理

## 10.1 当前异常处理策略

| 场景 | 当前处理 |
|:---|:---|
| 本地文件不存在 | 返回 `400`，带明确中文错误 |
| `.xls` 依赖缺失 | 提示安装 `xlrd` |
| `.xlsx` 依赖缺失 | 提示安装 `openpyxl` |
| 规则引用未知 tag | 返回 `400` |
| 规则依赖失败数据源 | 规则被过滤，不阻断整次执行 |
| 固定规则列不存在 | 保存配置时直接失败 |
| `svn` CLI 缺失 | 返回明确安装/配置提示 |
| 目标目录不是 SVN 工作副本 | 返回明确错误 |
| 飞书读取 | 当前直接 `NotImplementedError` |

## 10.2 当前性能策略

- Excel / CSV 读取优先使用 `usecols`
- 多数据源读取采用线程池并发
- 规则执行采用 Pandas 列级运算
- 统一结果结构避免重复转换成本

## 10.3 当前安全与运行约束

- 本地文件选择依赖桌面环境和 `tkinter`
- 当前更适合本机开发机或桌面联调环境
- 若部署到无桌面的服务环境，`local-pick` 不适用

---

## 11. 当前验证基线

## 11.1 构建与测试

当前已验证：

- `python -m pytest backend/tests -q` → `21 passed`
- `cd frontend && npm run build` → 通过

## 11.2 默认启动命令

### 后端

```powershell
python backend/run.py
```

### 前端

```powershell
cd frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

## 11.3 默认访问地址

- 前端工作台：`http://127.0.0.1:5173`
- 固定规则页：`http://127.0.0.1:5173/fixed-rules`
- 后端健康检查：`http://127.0.0.1:8000/health`
- 后端文档：`http://127.0.0.1:8000/docs`

## 11.4 当前已验证样例

### 主工作台最小样例

结果：

- `Execution Completed`
- `total_rows_scanned = 8`
- `failed_sources = []`
- `abnormal_results = 5`

### qa88 主工作台联调

结果：

- `Execution Completed`
- `total_rows_scanned = 5410`
- `failed_sources = []`
- `abnormal_results = 11`

### 固定规则验收样例

样例：

- `D:\projact_samo\GameDatas\datas_qa88\items.xls -> items -> INT_ID > 0`

结果：

- `Execution Completed`
- `total_rows_scanned = 3917`
- `failed_sources = []`
- `abnormal_results = 0`

### 固定规则反向压测

样例：

- `INT_ID > 10000`

结果：

- `Execution Completed`
- `total_rows_scanned = 3917`
- `failed_sources = []`
- `abnormal_results = 770`

---

## 12. 当前遗留项与后续建议

## 12.1 当前遗留项

- `feishu` 数据源真实读取未实现
- `regex` 规则未闭环
- `svn` 当前依赖本机 CLI，且当前环境未安装
- CSV 变量池元数据下拉未开放
- 固定规则模块尚不支持多配置集
- 固定规则结果导出未实现

## 12.2 后续建议优先级

### P1

- 完成 `svn` CLI 安装检测、绝对路径配置和工作副本校验提示优化
- 为固定规则模块增加结果导出

### P2

- 补齐 `regex` 规则闭环
- 为 CSV 增加元数据下拉提取

### P3

- 设计固定规则多配置集
- 评估恢复动态规则平台时的前端入口与协议策略

---

## 13. 结论

当前系统已经从“单一四步工作台”演进为“两条校验主链路并存”的状态：

1. `/` 工作台负责临时编排
2. `/fixed-rules` 负责长期维护固定规则

后端继续坚持统一执行模型：

- 统一 `TaskTree`
- 统一规则注册表
- 统一响应格式

固定规则模块当前采用“固定规则页自己的数据源与变量池 + `version = 4` 单配置持久化 + 一键执行全部规则”的设计，已经是项目中最接近业务长期使用形态的模块。后续演进建议优先围绕 `SVN`、`regex`、导出和多配置集展开，而不是重新拆分执行引擎。
